import { Card, CardContent, CardHeader } from '@material-ui/core'

export default () => (
    <Card>
        <CardHeader title="Welcom to the administration"/>
        <CardContent>bula bula</CardContent>
    </Card>
)